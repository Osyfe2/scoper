[package]
edition = "2024"
name = "scoper-noop"
version = "0.2.0"

[dependencies]
scoper-attr = { package = "scoper-noop-attr", path = "../scoper-noop-attr" }
scoper-base ={ workspace = true}
const_format = { workspace = true }
